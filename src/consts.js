/**
 * Application constants
 */
export default {
    /**
     * square Shape default(restrictions) params
     */
    square: {
        MIN_WIDTH: 20,
        MAX_WIDTH: 150,
    },

    /**
     * circle Shape default(restrictions) params
     */
    circle: {
        MIN_WIDTH: 30,
        MAX_WIDTH: 200,
    },
};